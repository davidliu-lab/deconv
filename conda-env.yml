# to create env:
# conda env create --file conda-env.yml

# to update env:
# conda env update --name deconv --file conda-env.yml

name: deconv
channels:
  - conda-forge
  - defaults
dependencies:
  - docker-py
  - gcsfs
  - google-cloud-storage
  - jupyterlab-lsp
  - matplotlib
  - pandas>=1.4
  - pandas-gbq>=0.17.4
  - pandas-profiling
  - pip
  - plotly>=5.6
  - pyarrow
  - python>=3.10
  - scikit-learn
  - scipy
  - seaborn
  - pip:
    - black[jupyter]
    - GEOparse
    - gsea-api
    - isort
    - kaleido
    - rope

    # - git+https://github.com/googleapis/python-db-dtypes-pandas.git
    # - git+https://github.com/googleapis/python-bigquery-pandas.git
